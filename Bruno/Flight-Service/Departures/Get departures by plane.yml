info:
  name: Get departures by plane
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:3000/api/departures
  body:
    type: json
    data: |-
      {
        "planeReg": "CS-TVI"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
