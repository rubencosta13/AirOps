info:
  name: Get Departures
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:3000/api/departures?planeReg=CS-TVI
  params:
    - name: planeReg
      value: CS-TVI
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
