info:
  name: Get Tasks
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
