info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3005/api/auth/logout
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
