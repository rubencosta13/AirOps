info:
  name: Forgot Password
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3005/api/auth/forgot-password
  body:
    type: json
    data: |-
      {
        "email": "test@test.com"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
