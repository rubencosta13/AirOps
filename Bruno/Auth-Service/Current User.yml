info:
  name: Current User
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3005/api/auth/me
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "123"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
